# One shared template for both stacks/profiles in docker-compose.yml. Each service only reads
# the variables it references, so it's safe to fill in only the section relevant to whichever
# host you're deploying to and leave the other section as "changeme" -- copy this whole file to
# `.env` on each host either way.

# -------------------------------------------------
# Pipeline (docker-compose.yml, --profile pipeline -- postgres-projects + ingest-worker,
# the n8n/Flowise host)
# -------------------------------------------------

# Port ingest-worker's API listens on (both inside the container and the host-side publish) --
# only needs to change if 8080 is already taken by something else on this host.
INGEST_WORKER_API_PORT=8080

POSTGRES_PROJECTS_PASSWORD=changeme

# Same broker Frigate itself publishes frigate/events to -- not recorded anywhere else in this
# repo (n8n's old MQTT Trigger just referenced a saved credential by name), fill in for real.
MQTT_HOST=changeme
MQTT_PORT=1883
MQTT_USERNAME=changeme
MQTT_PASSWORD=changeme
# Optional camera allow-list (comma-separated Frigate camera names, e.g. "outside,outside2") --
# applies to both the events flow (frigate/events) and the alerts flow (frigate/reviews). A
# camera not on this list never gets a raw_events/visits row at all. Leave blank/unset (default)
# to process every camera Frigate reports.
# CAMERAS=outside,outside2

# Frigate's REST API, reachable cross-host from wherever ingest-worker runs (its LAN-bound port,
# not a Docker service name unless both are on the same Docker network).
FRIGATE_API_BASE=http://192.168.1.10:5000

# Shared secret for ingest-worker's read/query/report API (/events, /sightings, /stats, /reports --
# sent as an X-API-Key header). Not required for /health, /status, /crop/{id}, /retention/run.
API_KEY=changeme

# Camera hardware facts -- not a tuning knob, must match your actual record-stream resolution.
# RECORD_WIDTH=3840
# RECORD_HEIGHT=2160

# NOTE: everything else that used to live here -- crop tuning, queue tuning (parallel limits,
# retry counts, timeouts, poll intervals), retention, video storage, visit previews, Telegram
# modes, and the internal AI stages -- is configured entirely in profiles.yaml now, under its one
# `defaults:` section (per-object-type settings like crop_disabled/store_video/
# telegram_events_mode/ai_events_stage_enabled can also be overridden per type there;
# non-per-type technical knobs like parallel_limit/retention_months/ai_stage_max_attempts apply
# globally only). See configuration.md's "Per-object-type overrides" section and profiles.yaml's
# own comments for the full list and defaults. This file now covers only connection info, paths,
# tokens, and other genuinely external/per-host facts.

# Video storage (third queue stage, video_status) -- store_video itself is configured in
# profiles.yaml. Clips are downloaded from Frigate's own clip.mp4 endpoint, stored under
# VIDEO_STORAGE_HOST_PATH on the host (bind-mounted to /data/video in the container), only the
# path lives in Postgres.
# Host-side directory for stored clips -- relative paths resolve against docker-compose.yml's
# location (frigate/), so the default below is frigate/video-storage/.
VIDEO_STORAGE_HOST_PATH=./video-storage

# Independent video-storage switch for the alerts/visits flow (frigate/reviews) -- one clip per
# visit's whole start_ts->end_ts span, separate from store_video above (which only covers the
# events flow, one clip per raw_event). store_video_alerts itself is configured in profiles.yaml.
# Host-side directory for stored visit clips -- a genuinely separate location from
# VIDEO_STORAGE_HOST_PATH above (own bind mount), not a subfolder of it, so the two flows' disk
# usage can be measured/managed independently. Default resolves to frigate/video-storage-alerts/.
VIDEO_STORAGE_ALERTS_HOST_PATH=./video-storage-alerts

# Telegram notifications -- telegram_events_mode/telegram_alerts_mode are both configured in
# profiles.yaml, not here. TELEGRAM_BOT_TOKEN/TELEGRAM_CHAT_ID only need to be real values once
# either mode is anything other than "none" somewhere in profiles.yaml.
TELEGRAM_BOT_TOKEN=changeme
TELEGRAM_CHAT_ID=changeme
# Defaults to Telegram's own cloud API. Point at the optional telegram-bot-api Compose service
# below instead (http://telegram-bot-api:8081) for lower latency and a 2000MB upload cap instead
# of the cloud API's 50MB -- worth it once store_video/store_video_alerts video clips are large
# enough to hit that cap.
TELEGRAM_API_BASE_URL=https://api.telegram.org

# Object labels your Frigate config actually produces -- drives the web UI's Type filter dropdown
# (GET /object-types), so a label you add here shows up in the UI without a frontend change.
OBJECT_TYPES=car,truck,person,dog

# Internal AI stages (ai_worker.py / alert_ai_worker.py) -- the only AI-stage implementation in
# this project now (n8n/metadata-processor.json was deleted as stale -- see CLAUDE.md's "Internal
# AI stage" section). Two independent stages (ai_events_stage_enabled/ai_alerts_enabled),
# configured entirely in profiles.yaml, not here.
# Object types + prompts/model per type also live in frigate/profiles.yaml (repo root,
# bind-mounted by docker-compose.yml -- just edit and restart, no rebuild needed).
AI_STAGE_PROFILE_PATH=/app/profiles.yaml
# The `provider: llama_proxy` (default) target's base URL/token, called directly instead of going
# through n8n -- only required when ai_events_stage_enabled or ai_alerts_enabled is true for at
# least one type in profiles.yaml. Despite the name, this can point at ANY server speaking an
# OpenAI-compatible /v1/chat/completions API -- llama_slot_proxy (multiple model slots behind one
# host, profiles.yaml's chat_path picks the slot) is the common case, but a bare llama.cpp
# `llama-server` works too with no proxy in front (LLAMA_PROXY_BASE_URL=http://llama-cpp-host:8080,
# chat_path: /v1/chat/completions -- llama.cpp's own fixed route). See profiles.yaml.example's
# "provider" comment (shapes 1a/1b) for both wired up side by side. LLAMA_PROXY_TOKEN is optional
# (blank = no Authorization header sent -- unauthenticated on the LAN is the common case for both),
# and applies to BOTH the chat endpoint (chat_path) and the embedding endpoint
# (LLAMA_PROXY_EMBED_PATH below) -- one token for the one host, same as OPENAI_API_KEY further
# down is one key shared by OpenAI's own chat and embedding endpoints.
LLAMA_PROXY_BASE_URL=
LLAMA_PROXY_TOKEN=
LLAMA_PROXY_EMBED_PATH=/REPLACE_WITH_EMBED_SLOT/v1/embeddings
# Must match the output size of whatever model is loaded behind LLAMA_PROXY_EMBED_PATH (1024 for
# Qwen3-Embedding-0.6B-GGUF, 768 for nomic-embed-text-v1.5, etc). Sizes the pgvector embedding
# columns on sightings/visit_sightings. Changing this after sightings already have
# embeddings stored clears them (a different model's vectors are an incomparable vector space
# regardless of dimension) -- re-run POST /embeddings/backfill?confirm=true afterwards. Stays a
# plain env var (unlike the technical tuning knobs mentioned above) -- db.ensure_schema() reads
# this before profiles.yaml is even loaded, and changing it has real DB-migration implications.
EMBEDDING_DIMENSIONS=1024

# Optional hosted VLM providers (OpenAI / Claude) -- an alternative to llama_slot_proxy for
# individual object types. Nothing below is required unless profiles.yaml sets `provider: openai`
# or `provider: anthropic` on at least one object_types entry (see profiles.yaml.example's
# top-of-file comment) -- llama_proxy above stays the default for every type that doesn't set
# `provider` at all. See docs/configuration.md's "Internal AI stages" section and CLAUDE.md's
# "Cloud VLM providers" section for which provider tends to suit which kind of description task.
#
# OPENAI_API_KEY -- from https://platform.openai.com/api-keys. Required only for a type using
# `provider: openai`, OR if EMBEDDING_PROVIDER (below) is set to openai.
# OPENAI_BASE_URL -- override only for an OpenAI-compatible proxy/gateway that isn't OpenAI's own
# api.openai.com (e.g. an internal LiteLLM/OpenRouter-style router) -- leave blank for real OpenAI.
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com
#
# ANTHROPIC_API_KEY -- from https://console.anthropic.com/settings/keys. Required only for a type
# using `provider: anthropic`. Claude has no embeddings API at all, so this is never read by
# EMBEDDING_PROVIDER below -- a type routed to Claude for its description still needs
# EMBEDDING_PROVIDER=llama_proxy (default) or openai for that sighting's embedding.
# ANTHROPIC_BASE_URL / ANTHROPIC_VERSION -- leave both at their defaults unless you're pointing at
# a proxy that fronts the real Anthropic API.
ANTHROPIC_API_KEY=
ANTHROPIC_BASE_URL=https://api.anthropic.com
ANTHROPIC_VERSION=2023-06-01
#
# EMBEDDING_PROVIDER -- "llama_proxy" (default, uses LLAMA_PROXY_BASE_URL/LLAMA_PROXY_EMBED_PATH/
# LLAMA_PROXY_TOKEN above -- the token is sent as a Bearer Authorization header on the embedding
# call too, not just the chat call) or "openai" (uses OPENAI_API_KEY/OPENAI_BASE_URL above +
# OPENAI_EMBED_MODEL below). Independent of whichever provider(s) profiles.yaml routes individual
# object types' chat calls to -- this only affects semantic search's embedding step
# (POST /search/semantic, POST /embeddings/backfill, the web UI's Search tab), never which model
# writes a sighting's description text.
EMBEDDING_PROVIDER=llama_proxy
# Only read when EMBEDDING_PROVIDER=openai. text-embedding-3-small is 1536 dimensions -- if you
# switch to it, also set EMBEDDING_DIMENSIONS=1536 above and re-run
# POST /embeddings/backfill?confirm=true (see the EMBEDDING_DIMENSIONS comment above for why).
OPENAI_EMBED_MODEL=text-embedding-3-small

# Optional local MQTT broker (mosquitto) -- its own compose profile, fully opt-in:
#   docker compose --profile pipeline --profile mqtt up -d
# then set MQTT_HOST=mosquitto (Compose service-name DNS) / MQTT_PORT=1883 above instead of an
# external broker's IP, if you want everything running from this one compose file.

# Optional self-hosted Telegram Bot API server -- its own compose profile, fully opt-in:
#   docker compose --profile pipeline --profile telegram-bot-api up -d
# then set TELEGRAM_API_BASE_URL=http://telegram-bot-api:8081 above instead of the default
# api.telegram.org. Get these from https://my.telegram.org (a Telegram account credential, not
# the bot token above -- the Local Bot API server needs it to talk to Telegram's backend directly).
TELEGRAM_API_ID=changeme
TELEGRAM_API_HASH=changeme

# -------------------------------------------------
# Frigate NVR stack (docker-compose.yml, --profile nvr -- the Frigate/camera host)
# -------------------------------------------------

FRIGATE_MQTT_PASSWORD=changeme
FRIGATE_RTSP_PASSWORD=changeme

# LAN IP this Frigate host binds its RTSP/WebRTC ports to (its own machine's IP, not a remote one).
FRIGATE_HOST_IP=192.168.1.10
# Broker Frigate connects OUT to -- often the same host as FRIGATE_HOST_IP, but not required to be.
FRIGATE_MQTT_HOST=192.168.1.10
# Camera IPs referenced by go2rtc streams in frigate.conf.
FRIGATE_CAM1_IP=192.168.1.20
FRIGATE_CAM2_IP=192.168.1.21
